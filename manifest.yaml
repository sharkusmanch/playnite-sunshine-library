AddonId: SunshineLibrary_496637e1-1607-4016-aa4b-d6f732c21210
Packages:
  - Version: 0.0.2
    RequiredApiVersion: 6.12.0
    ReleaseDate: 2026-04-13
    PackageUrl: https://github.com/sharkusmanch/playnite-sunshine-library/releases/download/v0.0.2/SunshineLibrary_v0.0.2.pext
    Changelog:
      - 'Fix Apollo 401: replace Basic auth with session-cookie login'
      - 'Address Claude review: disposal, login-failure short-circuit, ToGeneric guard'
      - 'Fix server-type probe for Apollo: retry with session auth on 401'
      - 'Style: normalize switch case alignment in ToGeneric'
  - Version: 0.0.1
    RequiredApiVersion: 6.12.0
    ReleaseDate: 2026-04-13
    PackageUrl: https://github.com/sharkusmanch/playnite-sunshine-library/releases/download/v0.0.1/SunshineLibrary_v0.0.1.pext
    Changelog:
      - Initial release — import apps from Sunshine/Apollo hosts, launch via Moonlight
      - Auto-detect client resolution, refresh rate, and HDR at launch
      - Per-host SPKI certificate pinning with TOFU confirmation
      - Sunshine vs Apollo auto-detection
      - Per-host and global streaming overrides (resolution, FPS, HDR, bitrate, codec, and more)
      - Per-game streaming overrides via right-click menu with bulk multi-select support
      - Cache fallback — offline hosts serve last-good app list tagged offline
      - Orphan reconciliation preserves playtime when app IDs change
      - Per-host auto-remove orphaned games setting (overrides global)
      - Fix stale orphan deletion when auto-remove is enabled after prior syncs
      - Apollo sync companion and Apollo Profile Manager documented
